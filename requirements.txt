# Python 3.12
#
# Pinned to the versions the reported results were produced with. Later
# versions of transformers rename TrainingArguments parameters; train_sft.py
# inspects the installed signature and drops arguments it does not accept, so
# it tolerates drift, but the figures in the dissertation come from these.

# --- model training and inference
torch==2.13.0
transformers==5.15.1
accelerate>=1.0.0
safetensors>=0.4.0
tokenizers>=0.20.0

# --- driving Isabelle over LSP (extraction and proof-completion evaluation)
isabelle-lsp-client==0.0.2
lsp-client>=0.1.0
pydantic>=2.0.0

# --- model download
huggingface_hub>=0.26.0
